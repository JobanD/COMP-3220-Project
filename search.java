// this class is for applying a search on the db
// applies the search plus a filter if wanted and sends info to accessDB to display it
// methods include adding a search which could be for name, date, id, data, etc and possibility of filter(s) taken from filter class

class search{
    public static void main(String args[]){
        
    }
}